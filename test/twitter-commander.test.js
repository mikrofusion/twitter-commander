'use strict';

var expect = require('chai').expect;
var sinon = require('sinon');

//var twitterCommander = require('../lib/twitter-commander.js');

describe('twitterCommander', function () {
  describe('run', function() {
    describe('when no creds are given', function() {

    });

    describe('when creds are given', function() {

    });
  });
});
